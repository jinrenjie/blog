---
title: Default File Opening Methods on macOS
date: 2023-07-12T09:36:01+08:00
author: George
authorTwitter: GeorgeBornAgain
cover: covers/20230712-default-app-macos.jpg
tags: [macOS, Utility, CLI, duti]
keywords: [macOS, Utility, CLI, duti]
description: Have you ever experienced double-clicking any type of code file on macOS and it always opens with Xcode? duti is designed to solve this problem.
showFullContent: false
readingTime: true
draft: false
---

# Introduction

As a developer working on macOS, I often encounter situations where double-clicking a code file results in a long wait only to have it open in Xcode. Even after manually setting the default application for certain file types, the settings don't take effect. This experience is quite frustrating.

By chance, I saw someone on Twitter using a tool called [duti](https://github.com/moretension/duti) to set default file opening methods for macOS.

# Installation

{{< prismjs lang=bash command-line=true prompt=$ >}}
brew install duti
{{< /prismjs >}}

# Usage
{{< prismjs lang=bash command-line=true prompt=$ output="2-4" >}}
duti -h
usage: duti [ -hvV ] [ -d uti ] [ -l uti ] [ settings_path ]
usage: duti -s bundle_id { uti | url_scheme } [ role ]
usage: duti -x extension
{{< /prismjs >}}

You can use the following command to set the default application for opening a certain type of file:

{{< prismjs lang=bash command-line=true prompt=$ >}}
duti -s bundle_id { uti | url_scheme } [ role ]
{{< /prismjs >}}

To get the Bundle ID of an application, you can use the following command:

{{< prismjs lang=bash command-line=true prompt=$ >}}
osascript -e 'id of app "APP_NAME"'
{{< /prismjs >}}

> Replace `APP_NAME` with the name of the application you want to get.

For example, to get the Bundle ID of `Visual Studio Code`, you can execute the following command:

{{< prismjs lang=bash command-line=true prompt=$ output="2" >}}
osascript -e 'id of app "Visual Studio Code"'
com.microsoft.VSCode
{{< /prismjs >}}

> For information about [Uniform Type Identifiers(UTI)](https://developer.apple.com/library/content/documentation/FileManagement/Conceptual/understanding_utis/understand_utis_intro/understand_utis_intro.html), refer to Apple's documentation.

For example, if we want to open all `.conf` type files using Visual Studio Code, we can execute the following command:

{{< prismjs lang=bash command-line=true prompt=$ output="2-3,5-7" >}}
duti -s com.microsoft.VSCode .conf all

# Verify if successful
duti -x .conf
Visual Studio Code
/Applications/Visual Studio Code.app
com.microsoft.VSCode
{{< /prismjs >}}

![Finder](/article/20230712-finder-default-app.png)

Then when you double-click to open in Finder, you'll find that everything works normally!

I hope this is helpful, Happy hacking...